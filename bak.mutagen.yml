sync:
  defaults:
    ignore:
      vcs: true
      paths:
        - ".DS_Store"
        - "node_modules"
        - ".git"

  dotfiles:                # 会话名 = dotfiles
    alpha: "~/repo/dotfiles"
    beta:  "edward@14.103.135.152:~/repo/dotfiles"
    mode: "two-way-resolved"