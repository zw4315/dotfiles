# Neovim User Notes

Canonical docs moved to repository-level docs:

- `docs/NVIM.md`: keymap + workflow (single source)
- `docs/SETUP.md`: install and troubleshooting

This file stays as a compatibility pointer.
