# Preset 配置
# 格式: 预设名= 后每行一个组，空行或新预设名结束

# 最小安装 - 核心 + 编辑器 + 开发环境
minimal=
  core
  editors
  dev-env

# 开发完整（默认）- 包含开发工具
dev=
  core
  dev-tools
  editors
  dev-env
  system

# 全部安装 - 包含所有可选组件
full=
  core
  dev-tools
  editors
  dev-env
  system
  optional
